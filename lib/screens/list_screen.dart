import 'package:flutter/material.dart';

/// 一覧画面（タブ）。F7 の実装はここに入る。
class ListScreen extends StatelessWidget {
  const ListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('一覧画面', style: TextStyle(fontSize: 24)),
    );
  }
}
